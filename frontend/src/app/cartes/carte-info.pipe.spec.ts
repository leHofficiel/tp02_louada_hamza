import { CarteInfoPipe } from './carte-info.pipe';

describe('CarteInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new CarteInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
